name: Lifecycle registry hygiene

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lifecycle-registry:
    uses: Albert-Maxwell/.github/.github/workflows/lifecycle-registry.yml@3f130314c1e3a86528b26e22c177fd6370786b7b
